'use client';

interface TokenInfoCardProps {
  projectId: string;
}

export default function TokenInfoCard({ projectId }: TokenInfoCardProps) {
  //load all the info
  const name = 'TokenName';
  const symbol = 'TokenSymbol';
  const image = 'TokenImage';
  const mcap = 0;
  const vol = 0;
  const creator = 'TokenCreator';
  const xLink = 'TokenXLink';
  const tgLink = 'TokenTgLink';
  const projectLink = 'TokenProjectLink';

  return <div></div>;
}
